# ============================================
# Redis Database Container
# ============================================
# Use official Redis image based on Alpine Linux (lightweight)
FROM redis:alpine

# Expose port 6379 - the default Redis port
# This allows other containers/services to connect to Redis
EXPOSE 6379